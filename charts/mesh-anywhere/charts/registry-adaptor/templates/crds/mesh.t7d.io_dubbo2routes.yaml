apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: dubbo2routes.mesh.t7d.io
spec:
  group: mesh.t7d.io
  names:
    kind: Dubbo2Route
    listKind: Dubbo2RouteList
    plural: dubbo2routes
    singular: dubbo2route
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.meshService
      name: MeshService
      type: string
    - jsonPath: .status.lastSyncTime
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: Dubbo2Route is the Schema for the dubbo2routes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: Dubbo2RouteSpec defines the desired state of Dubbo2Route
            properties:
              meshService:
                type: string
              routes:
                items:
                  properties:
                    registryService:
                      type: string
                    toController:
                      properties:
                        name:
                          type: string
                        type:
                          type: string
                      required:
                      - name
                      - type
                      type: object
                  required:
                  - registryService
                  - toController
                  type: object
                type: array
            required:
            - meshService
            - routes
            type: object
          status:
            description: Dubbo2RouteStatus defines the observed state of Dubbo2Route
            properties:
              envoyFilters:
                items:
                  type: string
                type: array
              lastSyncTime:
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
